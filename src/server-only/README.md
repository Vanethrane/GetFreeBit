/**
 * Server-only Next.js route handlers (API, dynamic OG, chunked sitemaps).
 * Kept out of `src/app` so `output: "export"` can emit static HTML for Pages.
 * Restore under `src/app/` when deploying to a Node/Workers host that supports them.
 */
export {};
